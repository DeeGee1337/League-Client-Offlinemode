# League-Client-Offlinemode
appear offline

little program in c++ which searches for the proscess ID, IP entry in the windows TCP table and blocks it with a batch.
It was written is a couple of mins -> shitty code but it works.
